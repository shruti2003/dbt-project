-- models/intermediate/noc_region.sql

WITH noc_region AS (
    SELECT
        noc,
        region
    FROM
        {{ ref('noc_regions') }}  -- Reference the 'noc_regions' source table
)

-- Create the 'Noc_Region' table
SELECT * FROM noc_region
